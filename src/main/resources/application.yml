spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tgapp
    username: postgres
    password: do6rblubatman
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${PORT:8081}